#include "CommonDefinitions.h"

VEML_CONFIG ManualConfig80 = {
    .mode = MANUAL,
    .enabled = true,
    .exposureTime = MS80, 
    .triggerEnabled = true
};

VEML_CONFIG ManualConfig320 = {
    .mode = MANUAL,
    .enabled = true,
    .exposureTime = MS320, 
    .triggerEnabled = true
};
